apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "medplum.fullname" . }}-test-connection"
  labels:
    {{- include "medplum.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "medplum.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
