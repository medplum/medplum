/*
 * Generated by com.medplum.generator.Generator
 * Do not edit manually.
 */

package com.medplum.fhir.types;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.Test;

public class DetectedIssueTest {

    @Test
    public void testResourceType() {
        assertEquals("x", DetectedIssue.create().resourceType("x").build().resourceType());
    }

    @Test
    public void testId() {
        assertEquals("x", DetectedIssue.create().id("x").build().id());
    }

    @Test
    public void testMeta() {
        final Meta value = Meta.create().build();
        assertEquals(value, DetectedIssue.create().meta(value).build().meta());
    }

    @Test
    public void testImplicitRules() {
        assertEquals("x", DetectedIssue.create().implicitRules("x").build().implicitRules());
    }

    @Test
    public void testLanguage() {
        assertEquals("x", DetectedIssue.create().language("x").build().language());
    }

    @Test
    public void testText() {
        final Narrative value = Narrative.create().build();
        assertEquals(value, DetectedIssue.create().text(value).build().text());
    }

    @Test
    public void testContained() {
        final java.util.List<FhirResource> value = java.util.Collections.emptyList();
        assertEquals(value, DetectedIssue.create().contained(value).build().contained());
    }

    @Test
    public void testModifierExtension() {
        final java.util.List<Extension> value = java.util.Collections.emptyList();
        assertEquals(value, DetectedIssue.create().modifierExtension(value).build().modifierExtension());
    }

    @Test
    public void testIdentifier() {
        final java.util.List<Identifier> value = java.util.Collections.emptyList();
        assertEquals(value, DetectedIssue.create().identifier(value).build().identifier());
    }

    @Test
    public void testStatus() {
        assertEquals("x", DetectedIssue.create().status("x").build().status());
    }

    @Test
    public void testCode() {
        final CodeableConcept value = CodeableConcept.create().build();
        assertEquals(value, DetectedIssue.create().code(value).build().code());
    }

    @Test
    public void testSeverity() {
        assertEquals("x", DetectedIssue.create().severity("x").build().severity());
    }

    @Test
    public void testPatient() {
        final Reference value = Reference.create().build();
        assertEquals(value, DetectedIssue.create().patient(value).build().patient());
    }

    @Test
    public void testIdentifiedDateTime() {
        assertEquals("x", DetectedIssue.create().identifiedDateTime("x").build().identifiedDateTime());
    }

    @Test
    public void testIdentifiedPeriod() {
        final Period value = Period.create().build();
        assertEquals(value, DetectedIssue.create().identifiedPeriod(value).build().identifiedPeriod());
    }

    @Test
    public void testAuthor() {
        final Reference value = Reference.create().build();
        assertEquals(value, DetectedIssue.create().author(value).build().author());
    }

    @Test
    public void testImplicated() {
        final java.util.List<Reference> value = java.util.Collections.emptyList();
        assertEquals(value, DetectedIssue.create().implicated(value).build().implicated());
    }

    @Test
    public void testEvidence() {
        final java.util.List<DetectedIssue.DetectedIssueEvidence> value = java.util.Collections.emptyList();
        assertEquals(value, DetectedIssue.create().evidence(value).build().evidence());
    }

    @Test
    public void testDetail() {
        assertEquals("x", DetectedIssue.create().detail("x").build().detail());
    }

    @Test
    public void testReference() {
        assertEquals("x", DetectedIssue.create().reference("x").build().reference());
    }

    @Test
    public void testMitigation() {
        final java.util.List<DetectedIssue.DetectedIssueMitigation> value = java.util.Collections.emptyList();
        assertEquals(value, DetectedIssue.create().mitigation(value).build().mitigation());
    }
}
