{
  "name": "medplum-fsh-profiles",
  "version": "5.1.10",
  "private": true,
  "description": "Medplum FSH Profiles Example",
  "type": "module",
  "scripts": {
    "build": "npm run clean && npm run build:profiles",
    "build:profiles": "sushi build src/profiles -o dist -s",
    "clean": "rimraf dist",
    "dev": "npm run build:profiles"
  },
  "devDependencies": {
    "fsh-sushi": "3.19.0",
    "rimraf": "6.1.3"
  },
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": "^22.18.0 || >=24.2.0"
  }
}
