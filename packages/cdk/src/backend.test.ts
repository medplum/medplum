import { BackEnd } from './backend';

describe('BackEnd', () => {
  it('should do something', () => {
    const backend = new BackEnd();
    expect(backend).toBeInstanceOf(BackEnd);
  });

  // Add more "it" blocks as needed for each piece of functionality
});
