{{<master}}
{{$title}}Roles{{/title}}
{{$content}}
  <h1>Choose role</h1>
  {{error}}
  <form action="#" method="POST">
    <div class="scroll">
      {{#roles}}
      <div>
        <input type="radio" id="{{.}}" value="{{.}}" name="role">
        <label for="{{.}}">{{.}}</label>
      </div>
      {{/roles}}
    </div>
    <div>
      <input type="submit" value="Next">
    </div>
  </form>
{{/content}}
{{/master}}