{{<master}}
{{$title}}Scopes{{/title}}
{{$content}}
  <h1>Choose scopes</h1>
  {{error}}
  <form action="#" method="POST">
    <div class="scroll">
      {{#scopes}}
      <div>
        <input type="checkbox" id="{{.}}" name="{{.}}" checked>
        <label for="{{.}}">{{.}}</label>
      </div>
      {{/scopes}}
    </div>
    <div>
      <input type="submit" value="Next">
    </div>
  </form>
{{/content}}
{{/master}}