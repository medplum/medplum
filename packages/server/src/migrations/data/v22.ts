// SPDX-FileCopyrightText: Copyright Orangebot, Inc. and Medplum contributors
// SPDX-License-Identifier: Apache-2.0
import { getResourceTypes } from '@medplum/core';
import { PoolClient } from 'pg';
import { SYSTEM_PROJECT_ID } from '../../fhir/sql';
import { prepareCustomMigrationJobData, runCustomMigration } from '../../workers/post-deploy-migration';
import * as fns from '../migrate-functions';
import { withLongRunningDatabaseClient } from '../migration-utils';
import { MigrationActionResult } from '../types';
import { CustomPostDeployMigration } from './types';

export const migration: CustomPostDeployMigration = {
  type: 'custom',
  prepareJobData: (asyncJob) => prepareCustomMigrationJobData(asyncJob),
  run: async (repo, job, jobData) => {
    return runCustomMigration(repo, job, jobData, async () => {
      return withLongRunningDatabaseClient(async (client) => {
        const results: MigrationActionResult[] = [];
        await run(client, results);
        return results;
      });
    });
  },
};

// prettier-ignore
async function run(client: PoolClient, results: MigrationActionResult[]): Promise<void> {
  const resourceTypes = getResourceTypes();
  for (const resourceType of resourceTypes) {
    await fns.query(client, results, `UPDATE "${resourceType}" SET "projectId" = $1 WHERE "projectId" IS NULL`, [SYSTEM_PROJECT_ID]);
  }

  await fns.nonBlockingAlterColumnNotNull(client, results, `Account`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ActivityDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `AdverseEvent`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `AllergyIntolerance`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Appointment`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `AppointmentResponse`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `AuditEvent`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Basic`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Binary`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `BiologicallyDerivedProduct`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `BodyStructure`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Bundle`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CapabilityStatement`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CarePlan`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CareTeam`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CatalogEntry`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ChargeItem`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ChargeItemDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Claim`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ClaimResponse`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ClinicalImpression`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CodeSystem`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Communication`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CommunicationRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CompartmentDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Composition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ConceptMap`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Condition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Consent`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Contract`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Coverage`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CoverageEligibilityRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `CoverageEligibilityResponse`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DetectedIssue`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Device`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DeviceDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DeviceMetric`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DeviceRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DeviceUseStatement`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DiagnosticReport`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DocumentManifest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DocumentReference`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `EffectEvidenceSynthesis`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Encounter`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Endpoint`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `EnrollmentRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `EnrollmentResponse`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `EpisodeOfCare`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `EventDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Evidence`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `EvidenceVariable`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ExampleScenario`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ExplanationOfBenefit`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `FamilyMemberHistory`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Flag`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Goal`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `GraphDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Group`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `GuidanceResponse`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `HealthcareService`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ImagingStudy`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Immunization`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ImmunizationEvaluation`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ImmunizationRecommendation`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ImplementationGuide`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `InsurancePlan`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Invoice`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Library`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Linkage`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `List`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Location`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Measure`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MeasureReport`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Media`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Medication`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicationAdministration`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicationDispense`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicationKnowledge`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicationRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicationStatement`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProduct`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductAuthorization`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductContraindication`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductIndication`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductIngredient`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductInteraction`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductManufactured`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductPackaged`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductPharmaceutical`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MedicinalProductUndesirableEffect`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MessageDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MessageHeader`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `MolecularSequence`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `NamingSystem`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `NutritionOrder`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Observation`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ObservationDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `OperationDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `OperationOutcome`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Organization`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `OrganizationAffiliation`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Parameters`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Patient`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `PaymentNotice`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `PaymentReconciliation`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Person`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `PlanDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Practitioner`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `PractitionerRole`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Procedure`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Provenance`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Questionnaire`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `QuestionnaireResponse`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `RelatedPerson`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `RequestGroup`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ResearchDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ResearchElementDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ResearchStudy`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ResearchSubject`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `RiskAssessment`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `RiskEvidenceSynthesis`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Schedule`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SearchParameter`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ServiceRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Slot`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Specimen`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SpecimenDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `StructureDefinition`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `StructureMap`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Subscription`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SubscriptionStatus`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Substance`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SubstanceNucleicAcid`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SubstancePolymer`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SubstanceProtein`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SubstanceReferenceInformation`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SubstanceSourceMaterial`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SubstanceSpecification`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SupplyDelivery`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SupplyRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Task`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `TerminologyCapabilities`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `TestReport`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `TestScript`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ValueSet`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `VerificationResult`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `VisionPrescription`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Project`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ClientApplication`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `User`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `ProjectMembership`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Bot`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Login`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `PasswordChangeRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `UserSecurityRequest`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `JsonWebKey`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `AccessPolicy`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `UserConfiguration`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `BulkDataExport`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `SmartAppLaunch`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `DomainConfiguration`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `AsyncJob`, `projectId`);
  await fns.nonBlockingAlterColumnNotNull(client, results, `Agent`, `projectId`);
}
