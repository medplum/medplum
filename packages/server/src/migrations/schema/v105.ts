// SPDX-FileCopyrightText: Copyright Orangebot, Inc. and Medplum contributors
// SPDX-License-Identifier: Apache-2.0
/*
 * This is a generated file
 * Do not edit manually.
 */

import type { PoolClient } from 'pg';
import * as fns from '../migrate-functions';

// prettier-ignore
export async function run(client: PoolClient): Promise<void> {
  const results: { name: string; durationMs: number }[] = []
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Account" ADD COLUMN IF NOT EXISTS "__period" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Account" ADD COLUMN IF NOT EXISTS "__periodSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ActivityDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ActivityDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ActivityDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ActivityDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ActivityDefinition" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ActivityDefinition" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AdverseEvent" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AdverseEvent" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AllergyIntolerance" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AllergyIntolerance" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AllergyIntolerance" ADD COLUMN IF NOT EXISTS "__lastDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AllergyIntolerance" ADD COLUMN IF NOT EXISTS "__lastDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AllergyIntolerance" ADD COLUMN IF NOT EXISTS "__onset" TSTZMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AllergyIntolerance" ADD COLUMN IF NOT EXISTS "__onsetSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Appointment" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Appointment" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Appointment" ADD COLUMN IF NOT EXISTS "__end" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Appointment" ADD COLUMN IF NOT EXISTS "__endSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AuditEvent" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "AuditEvent" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Basic" ADD COLUMN IF NOT EXISTS "__created" DATERANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Basic" ADD COLUMN IF NOT EXISTS "__createdSort" DATE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Bundle" ADD COLUMN IF NOT EXISTS "__timestamp" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Bundle" ADD COLUMN IF NOT EXISTS "__timestampSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CapabilityStatement" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CapabilityStatement" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CapabilityStatement" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CapabilityStatement" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CarePlan" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CarePlan" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CarePlan" ADD COLUMN IF NOT EXISTS "__activityDate" TSTZMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CarePlan" ADD COLUMN IF NOT EXISTS "__activityDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CareTeam" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CareTeam" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__enteredDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__enteredDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__factorOverride" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__factorOverrideSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__occurrence" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__occurrenceSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__priceOverride" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__priceOverrideSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__quantity" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItem" ADD COLUMN IF NOT EXISTS "__quantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItemDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItemDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItemDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItemDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItemDefinition" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ChargeItemDefinition" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Claim" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Claim" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ClaimResponse" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ClaimResponse" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ClaimResponse" ADD COLUMN IF NOT EXISTS "__paymentDate" DATERANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ClaimResponse" ADD COLUMN IF NOT EXISTS "__paymentDateSort" DATE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ClinicalImpression" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ClinicalImpression" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CodeSystem" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CodeSystem" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CodeSystem" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CodeSystem" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Communication" ADD COLUMN IF NOT EXISTS "__received" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Communication" ADD COLUMN IF NOT EXISTS "__receivedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Communication" ADD COLUMN IF NOT EXISTS "__sent" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Communication" ADD COLUMN IF NOT EXISTS "__sentSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Communication" ADD COLUMN IF NOT EXISTS "__priorityOrder" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Communication" ADD COLUMN IF NOT EXISTS "__priorityOrderSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CommunicationRequest" ADD COLUMN IF NOT EXISTS "__authored" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CommunicationRequest" ADD COLUMN IF NOT EXISTS "__authoredSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CommunicationRequest" ADD COLUMN IF NOT EXISTS "__occurrence" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CommunicationRequest" ADD COLUMN IF NOT EXISTS "__occurrenceSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CommunicationRequest" ADD COLUMN IF NOT EXISTS "__priorityOrder" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CommunicationRequest" ADD COLUMN IF NOT EXISTS "__priorityOrderSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CompartmentDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CompartmentDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CompartmentDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CompartmentDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Composition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Composition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Composition" ADD COLUMN IF NOT EXISTS "__period" TSTZMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Composition" ADD COLUMN IF NOT EXISTS "__periodSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ConceptMap" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ConceptMap" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ConceptMap" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ConceptMap" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__abatementAge" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__abatementAgeSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__abatementDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__abatementDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__onsetAge" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__onsetAgeSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__onsetDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__onsetDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__recordedDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__recordedDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__assertedDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Condition" ADD COLUMN IF NOT EXISTS "__assertedDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Consent" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Consent" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Consent" ADD COLUMN IF NOT EXISTS "__period" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Consent" ADD COLUMN IF NOT EXISTS "__periodSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Contract" ADD COLUMN IF NOT EXISTS "__issued" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Contract" ADD COLUMN IF NOT EXISTS "__issuedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CoverageEligibilityRequest" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CoverageEligibilityRequest" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CoverageEligibilityResponse" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "CoverageEligibilityResponse" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DetectedIssue" ADD COLUMN IF NOT EXISTS "__identified" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DetectedIssue" ADD COLUMN IF NOT EXISTS "__identifiedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DeviceRequest" ADD COLUMN IF NOT EXISTS "__authoredOn" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DeviceRequest" ADD COLUMN IF NOT EXISTS "__authoredOnSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DeviceRequest" ADD COLUMN IF NOT EXISTS "__eventDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DeviceRequest" ADD COLUMN IF NOT EXISTS "__eventDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DiagnosticReport" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DiagnosticReport" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DiagnosticReport" ADD COLUMN IF NOT EXISTS "__issued" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DiagnosticReport" ADD COLUMN IF NOT EXISTS "__issuedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DocumentManifest" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DocumentManifest" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DocumentReference" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DocumentReference" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DocumentReference" ADD COLUMN IF NOT EXISTS "__period" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "DocumentReference" ADD COLUMN IF NOT EXISTS "__periodSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EffectEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EffectEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EffectEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EffectEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EffectEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EffectEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Encounter" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Encounter" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Encounter" ADD COLUMN IF NOT EXISTS "__length" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Encounter" ADD COLUMN IF NOT EXISTS "__lengthSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Encounter" ADD COLUMN IF NOT EXISTS "__locationPeriod" TSTZMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Encounter" ADD COLUMN IF NOT EXISTS "__locationPeriodSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EpisodeOfCare" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EpisodeOfCare" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EventDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EventDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EventDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EventDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EventDefinition" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EventDefinition" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Evidence" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Evidence" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Evidence" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Evidence" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Evidence" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Evidence" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EvidenceVariable" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EvidenceVariable" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EvidenceVariable" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EvidenceVariable" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EvidenceVariable" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "EvidenceVariable" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ExampleScenario" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ExampleScenario" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ExampleScenario" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ExampleScenario" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ExplanationOfBenefit" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ExplanationOfBenefit" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "FamilyMemberHistory" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "FamilyMemberHistory" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Flag" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Flag" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Goal" ADD COLUMN IF NOT EXISTS "__startDate" DATERANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Goal" ADD COLUMN IF NOT EXISTS "__startDateSort" DATE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Goal" ADD COLUMN IF NOT EXISTS "__targetDate" DATEMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Goal" ADD COLUMN IF NOT EXISTS "__targetDateSort" DATE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "GraphDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "GraphDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "GraphDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "GraphDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Group" ADD COLUMN IF NOT EXISTS "__characteristicRange" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Group" ADD COLUMN IF NOT EXISTS "__characteristicRangeSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImagingStudy" ADD COLUMN IF NOT EXISTS "__started" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImagingStudy" ADD COLUMN IF NOT EXISTS "__startedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Immunization" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Immunization" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Immunization" ADD COLUMN IF NOT EXISTS "__reactionDate" TSTZMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Immunization" ADD COLUMN IF NOT EXISTS "__reactionDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImmunizationEvaluation" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImmunizationEvaluation" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImmunizationRecommendation" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImmunizationRecommendation" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImplementationGuide" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImplementationGuide" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImplementationGuide" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ImplementationGuide" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Invoice" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Invoice" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Invoice" ADD COLUMN IF NOT EXISTS "__totalgross" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Invoice" ADD COLUMN IF NOT EXISTS "__totalgrossSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Invoice" ADD COLUMN IF NOT EXISTS "__totalnet" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Invoice" ADD COLUMN IF NOT EXISTS "__totalnetSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Library" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Library" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Library" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Library" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Library" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Library" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "List" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "List" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Measure" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Measure" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Measure" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Measure" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Measure" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Measure" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MeasureReport" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MeasureReport" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MeasureReport" ADD COLUMN IF NOT EXISTS "__period" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MeasureReport" ADD COLUMN IF NOT EXISTS "__periodSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Media" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Media" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Medication" ADD COLUMN IF NOT EXISTS "__expirationDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Medication" ADD COLUMN IF NOT EXISTS "__expirationDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationAdministration" ADD COLUMN IF NOT EXISTS "__effectiveTime" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationAdministration" ADD COLUMN IF NOT EXISTS "__effectiveTimeSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationDispense" ADD COLUMN IF NOT EXISTS "__whenhandedover" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationDispense" ADD COLUMN IF NOT EXISTS "__whenhandedoverSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationDispense" ADD COLUMN IF NOT EXISTS "__whenprepared" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationDispense" ADD COLUMN IF NOT EXISTS "__whenpreparedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationRequest" ADD COLUMN IF NOT EXISTS "__authoredon" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationRequest" ADD COLUMN IF NOT EXISTS "__authoredonSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationRequest" ADD COLUMN IF NOT EXISTS "__date" TSTZMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationRequest" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationRequest" ADD COLUMN IF NOT EXISTS "__priorityOrder" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationRequest" ADD COLUMN IF NOT EXISTS "__priorityOrderSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationStatement" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MedicationStatement" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MessageDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MessageDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MessageDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MessageDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MolecularSequence" ADD COLUMN IF NOT EXISTS "__variantEnd" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MolecularSequence" ADD COLUMN IF NOT EXISTS "__variantEndSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MolecularSequence" ADD COLUMN IF NOT EXISTS "__variantStart" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MolecularSequence" ADD COLUMN IF NOT EXISTS "__variantStartSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MolecularSequence" ADD COLUMN IF NOT EXISTS "__windowEnd" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MolecularSequence" ADD COLUMN IF NOT EXISTS "__windowEndSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MolecularSequence" ADD COLUMN IF NOT EXISTS "__windowStart" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "MolecularSequence" ADD COLUMN IF NOT EXISTS "__windowStartSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "NamingSystem" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "NamingSystem" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "NamingSystem" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "NamingSystem" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "NamingSystem" ADD COLUMN IF NOT EXISTS "__period" TSTZMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "NamingSystem" ADD COLUMN IF NOT EXISTS "__periodSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "NutritionOrder" ADD COLUMN IF NOT EXISTS "__datetime" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "NutritionOrder" ADD COLUMN IF NOT EXISTS "__datetimeSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__comboValueQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__comboValueQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__componentValueQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__componentValueQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__valueDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__valueDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__valueQuantity" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Observation" ADD COLUMN IF NOT EXISTS "__valueQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "OperationDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "OperationDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "OperationDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "OperationDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "OrganizationAffiliation" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "OrganizationAffiliation" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Patient" ADD COLUMN IF NOT EXISTS "__birthdate" DATERANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Patient" ADD COLUMN IF NOT EXISTS "__birthdateSort" DATE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Patient" ADD COLUMN IF NOT EXISTS "__deathDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Patient" ADD COLUMN IF NOT EXISTS "__deathDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PaymentNotice" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PaymentNotice" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PaymentReconciliation" ADD COLUMN IF NOT EXISTS "__created" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PaymentReconciliation" ADD COLUMN IF NOT EXISTS "__createdSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Person" ADD COLUMN IF NOT EXISTS "__birthdate" DATERANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Person" ADD COLUMN IF NOT EXISTS "__birthdateSort" DATE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PlanDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PlanDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PlanDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PlanDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PlanDefinition" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PlanDefinition" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PractitionerRole" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "PractitionerRole" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Procedure" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Procedure" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Provenance" ADD COLUMN IF NOT EXISTS "__recorded" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Provenance" ADD COLUMN IF NOT EXISTS "__recordedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Provenance" ADD COLUMN IF NOT EXISTS "__when" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Provenance" ADD COLUMN IF NOT EXISTS "__whenSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Questionnaire" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Questionnaire" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Questionnaire" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Questionnaire" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Questionnaire" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Questionnaire" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "QuestionnaireResponse" ADD COLUMN IF NOT EXISTS "__authored" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "QuestionnaireResponse" ADD COLUMN IF NOT EXISTS "__authoredSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RelatedPerson" ADD COLUMN IF NOT EXISTS "__birthdate" DATERANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RelatedPerson" ADD COLUMN IF NOT EXISTS "__birthdateSort" DATE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RequestGroup" ADD COLUMN IF NOT EXISTS "__authored" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RequestGroup" ADD COLUMN IF NOT EXISTS "__authoredSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RequestGroup" ADD COLUMN IF NOT EXISTS "__priorityOrder" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RequestGroup" ADD COLUMN IF NOT EXISTS "__priorityOrderSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchDefinition" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchDefinition" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchElementDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchElementDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchElementDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchElementDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchElementDefinition" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchElementDefinition" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchStudy" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchStudy" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchStudy" ADD COLUMN IF NOT EXISTS "__recruitmentActual" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchStudy" ADD COLUMN IF NOT EXISTS "__recruitmentActualSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchStudy" ADD COLUMN IF NOT EXISTS "__recruitmentTarget" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchStudy" ADD COLUMN IF NOT EXISTS "__recruitmentTargetSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchSubject" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ResearchSubject" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskAssessment" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskAssessment" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskAssessment" ADD COLUMN IF NOT EXISTS "__probability" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskAssessment" ADD COLUMN IF NOT EXISTS "__probabilitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__effective" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "RiskEvidenceSynthesis" ADD COLUMN IF NOT EXISTS "__effectiveSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Schedule" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Schedule" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "SearchParameter" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "SearchParameter" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "SearchParameter" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "SearchParameter" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ServiceRequest" ADD COLUMN IF NOT EXISTS "__authored" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ServiceRequest" ADD COLUMN IF NOT EXISTS "__authoredSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ServiceRequest" ADD COLUMN IF NOT EXISTS "__occurrence" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ServiceRequest" ADD COLUMN IF NOT EXISTS "__occurrenceSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ServiceRequest" ADD COLUMN IF NOT EXISTS "__priorityOrder" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ServiceRequest" ADD COLUMN IF NOT EXISTS "__priorityOrderSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Slot" ADD COLUMN IF NOT EXISTS "__start" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Slot" ADD COLUMN IF NOT EXISTS "__startSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Slot" ADD COLUMN IF NOT EXISTS "__end" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Slot" ADD COLUMN IF NOT EXISTS "__endSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Specimen" ADD COLUMN IF NOT EXISTS "__collected" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Specimen" ADD COLUMN IF NOT EXISTS "__collectedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "StructureDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "StructureDefinition" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "StructureDefinition" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "StructureDefinition" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "StructureMap" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "StructureMap" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "StructureMap" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "StructureMap" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Substance" ADD COLUMN IF NOT EXISTS "__expiry" TSTZMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Substance" ADD COLUMN IF NOT EXISTS "__expirySort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Substance" ADD COLUMN IF NOT EXISTS "__quantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Substance" ADD COLUMN IF NOT EXISTS "__quantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "SupplyRequest" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "SupplyRequest" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__authoredOn" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__authoredOnSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__modified" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__modifiedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__period" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__periodSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__dueDate" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__dueDateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__priorityOrder" NUMRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "Task" ADD COLUMN IF NOT EXISTS "__priorityOrderSort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TerminologyCapabilities" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TerminologyCapabilities" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TerminologyCapabilities" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TerminologyCapabilities" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TestReport" ADD COLUMN IF NOT EXISTS "__issued" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TestReport" ADD COLUMN IF NOT EXISTS "__issuedSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TestScript" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TestScript" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TestScript" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "TestScript" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ValueSet" ADD COLUMN IF NOT EXISTS "__contextQuantity" NUMMULTIRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ValueSet" ADD COLUMN IF NOT EXISTS "__contextQuantitySort" NUMERIC`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ValueSet" ADD COLUMN IF NOT EXISTS "__date" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "ValueSet" ADD COLUMN IF NOT EXISTS "__dateSort" TIMESTAMPTZ`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "VisionPrescription" ADD COLUMN IF NOT EXISTS "__datewritten" TSTZRANGE`);
  await fns.query(client, results, `ALTER TABLE IF EXISTS "VisionPrescription" ADD COLUMN IF NOT EXISTS "__datewrittenSort" TIMESTAMPTZ`);
}
