/*
 * Generated by @medplum/generator
 * Do not edit manually.
 */

import { PoolClient } from 'pg';

export async function run(client: PoolClient): Promise<void> {
  await client.query(`ALTER TABLE IF EXISTS "Observation" SET (
    autovacuum_vacuum_scale_factor = 0.01, -- Default is 0.2 (20%)
    autovacuum_analyze_scale_factor = 0.005, -- Default is 0.1 (10%)
    autovacuum_vacuum_threshold = 1000, -- Default is 50
    autovacuum_analyze_threshold = 1000, -- Default is 50
    autovacuum_vacuum_cost_limit = 2000, -- Default is -1, which yields global value (global defaults to 200)
    autovacuum_vacuum_cost_delay = 10 -- Default is -1, which yields global value (global defaults to 0, which disables cost-based vacuuming)
  );`);
}
